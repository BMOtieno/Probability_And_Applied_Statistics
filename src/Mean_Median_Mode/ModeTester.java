package Mean_Median_Mode;

public class ModeTester {
    public static void main(String[] args) {
        //creating an instance of the Mode class
        Mode mode = new Mode();
        //call to the getUserInput method
        mode.getUserInput();
    }
}
